const User = require('../models/User');

class UserController {

    async createUser(request, reponse) {
        try {
            
        } catch (error) {
            
        }
    }

    async deleteUser(request, reponse) {
        try {
            
        } catch (error) {
            
        }
    }

    async changePassword(request, reponse) {
        try {
            
        } catch (error) {
            
        }
    }

    async getUser(request, reponse) {
        try {
            
        } catch (error) {
            
        }
    }
}

module.exports = new UserController();